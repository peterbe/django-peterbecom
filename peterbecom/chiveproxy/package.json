{
  "name": "chiveproxy",
  "version": "1.0.0",
  "author": "",
  "main": "html-getter.ts",
  "devDependencies": {
    "@types/bun": "^1.3.0"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "description": "",
  "keywords": [],
  "license": "ISC",
  "private": true,
  "scripts": {
    "compile": "bun build html-getter.ts --outfile out/html_getter --minify --sourcemap --bytecode --compile",
    "compile:linux-x64": "bun build html-getter.ts --outfile out/html_getter --minify --sourcemap --bytecode --compile --target=bun-linux-x64"
  }
}
