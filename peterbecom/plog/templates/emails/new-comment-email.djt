<!DOCTYPE html>
<html>
<head>
    <style>
        /* Inline styles work best for email */
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        .header {
            background-color: #4CAF50;
            color: white;
            padding: 20px;
            text-align: center;
        }
        .button {
            display: inline-block;
            padding: 10px 20px;
            background-color: #4CAF50;
            color: white;
            text-decoration: none;
            border-radius: 5px;
        }
        dt {
            font-weight: bold;
        }
        blockquote {
            border-left: 4px solid #ccc;
            padding-left: 10px;
            color: #555;
            margin-bottom: 40px;
        }
        dt, dd {
            display: inline;
            margin: 0; /* Remove default margins */
        }

        dt:not(:first-child):before {
            content: "\A"; /* Insert a line break before each <dt> except the first */
            white-space: pre; /* Ensure the line break is rendered */
        }

        dd:before {
            content: " "; /* Add a space between <dt> and <dd> */
        }
    </style>
</head>
<body>
    <div class="container">

        <p>
            <b>Post:</b> <a href="{{ absolute_url }}">{{ blogitem.title }}</a>
        </p>

        <dl>
            <dt>Name</dt>
            <dd>{% if blogcomment.name %}{{ blogcomment.name }}{% else %}<i>not set</i>{% endif %}</dd>
            <dt>Email</dt>
            <dd>{% if blogcomment.email %}{{ blogcomment.email }}{% else %}<i>not set</i>{% endif %}</dd>
            <dt>IP Address</dt>
            <dd>{{ blogcomment.ip_address }}</dd>
            <dt>User Agent</dt>
            <dd>{{ blogcomment.user_agent }}</dd>
        </dl>

        <h4>Comment:</h4>

        <blockquote>
            {{ blogcomment.comment|linebreaks }}
        </blockquote>

        <hr/>

        <p>
        <a href="{{ admin_url }}">View in Admin</a>
        </p>

    </div>
</body>
</html>