<!DOCTYPE html>
<html>
<head>
    <title>Peterbe.com: New comment</title>
    <style>
        /* Inline styles work best for email */
        body {
            font-family: Arial, sans-serif;
            line-height: 1.6;
            color: #333;
        }
        .container {
            max-width: 600px;
            margin: 0 auto;
            padding: 20px;
        }
        table th {
            text-align: left;
            white-space: nowrap;
            vertical-align: top;
        }
    </style>
</head>
<body>
    <div class="container">

        <p>
            <b>Post:</b> <a href="{{ absolute_url }}">{{ blogitem.title }}</a>
        </p>

        <table>
            <tbody>
                <tr>
                    <th>Name</th>
                    <td>{% if blogcomment.name %}{{ blogcomment.name }}{% else %}<i>not set</i>{% endif %}</td>
                </tr>
                <tr>
                    <th>Email</th>
                    <td>{% if blogcomment.email %}{{ blogcomment.email }}{% else %}<i>not set</i>{% endif %}</td>
                </tr>
                <tr>
                    <th>IP Address</th>
                    <td>{{ blogcomment.ip_address }}</td>
                </tr>
                <tr>
                    <th>User Agent</th>
                    <td>{{ blogcomment.user_agent }}</td>
                </tr>
            </tbody>
        </table>

        <h4>Comment</h4>

        <div style="font-style: italic">
            {{ blogcomment.comment|linebreaks }}
        </div>

        <hr/>

        <p>
        <a href="{{ admin_url }}">View in Admin</a>
        </p>

    </div>
</body>
</html>